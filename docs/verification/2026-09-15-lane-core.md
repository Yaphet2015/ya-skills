# Core lifecycle lane ledger — 2026-09-15

> Lane-local handoff snapshot at checkpoint `e7c172f57b2bdb846ad388ba4961757d969bd59a`. Outside-lane notes describe pre-integration ownership; current cumulative status is in `docs/verification/2026-09-15-parallel-integration.md`.

Status: implementation lane handoff, not a project-complete or release claim. The lane started from HEAD `e7c172f57b2bdb846ad388ba4961757d969bd59a` and kept all changes unstaged.

## Owned findings and evidence

| Finding | Evidence in this lane | Remaining gap |
|---|---|---|
| F1 | `exec-runner.ts` rechecks admission, cancellation, and the absolute run/batch deadline before every inner action and observation; later steps become `not_run`. `computer-lane-core-lifecycle.test.ts` covers delayed first-step timeout and terminal unawaited batch. | Native desktop timeout/cancel behavior is not established here; no desktop actions were run. |
| F2 | `boundedResult` preserves `unknown`/`interrupted`, compacts but retains every receipt, and reports omitted observations. The lane overflow + unknown-delivery test asserts unknown status, retained unknown receipt, zero returned observations, and session unusable. | A production result with every possible user-controlled field at the wire limit is not separately fuzzed. |
| F3 | Lease owners now retain unresolved worker process groups or a durable `cleanupUnproven` poison bit. Host exec/driver cleanup failures refresh those markers before retaining the lease; dead leader plus live group is not reclaimable. The lane owns a private detached fixture and asserts `target_busy`. | A native production worker whose process group ignores cleanup was not run; SIGKILL cannot be made to ignore in a desktop-free fixture. |
| F5 | Exec state commits write versioned `state/history/state-N.json`; recovery validates intent/hash against the historical version, not only the current head. Journal listing validates all commit intents before new admission. Tests cover a historical duplicate after a later commit and dead-writer crash after state rename before terminal event. | Power-loss/fsync semantics and a true process crash between individual filesystem syscalls remain unverified. |
| F6 | Runner keeps cancellation active through final observation and checks cancellation again before/while commit; cancellation clears pending commit. Lane test cancels while final observe is delayed and asserts no state file/commit. | Native driver cancellation after input crossed its boundary remains unknown by contract and is not desktop-tested. |
| F7 | Explicit, batch, and final observations share `registerObservation`; cap overflow is terminal and not silently truncated. Lane test drives 21 batch observations and asserts 20 retained plus `observation_limit`. | No large real AX/image payload was used; only desktop-free synthetic observations. |
| F8 | Host request deadlines begin before claim/journal work and are passed as absolute deadlines; hosted action journal waits, actions, and final observe use remaining time. Exec setup, RPCs, observations, final read, and commit checks use one absolute deadline. Lane hosted delayed-actions test asserts no third dispatch; exec inner timeout test asserts later `not_run`. | Runtime standalone/session deadline work and real native timing belong to the runtime lane. |
| F9 | Exec and hosted paths append one receipt per dispatched action, fill remaining steps as `not_run`, and preserve mixed single/batch run-level indices. Lane mixed-receipt test asserts indices `[0,1,2]` and statuses delivered/not_delivered/not_run; unawaited tests assert the later step never dispatches; a real self-spawned driver-worker crash after the first known result asserts delivered/unknown/not_run. | Native desktop crash behavior remains unverified; the subprocess fixture exercises the production host/driver boundary without SDK load. |
| F11 | `openSession` uses ignored stdio and `child.unref()` after the socket handshake; the opener lane spawns a real Bun opener process, waits for it to exit, then uses the still-live host socket. | Compiled Homebrew binary opener behavior was not run in this focused lane. |
| F12 (host side) | Finalized closed hosts remove the socket/listener; unusable hosts stay queryable while live, and `host.close()`/`hostMain` remove the final listener/path. Lane test verifies unusable status is queryable before explicit finalization and socket path is then gone. | Public CLI metadata fallback is intentionally unowned and remains a sibling lane responsibility. |

## Files changed by this lane

- `packages/computer-runtime/src/request-journal.ts`
- `packages/computer-runtime/src/target-lease.ts`
- `packages/computer-session/src/exec-runner.ts`
- `packages/computer-session/src/exec-state.ts`
- `packages/computer-session/src/host.ts`
- `packages/computer-session/src/index.ts`
- `packages/computer-session/src/open.ts`
- `packages/computer-session/src/process.ts`
- `tests/helpers/session-worker.ts`
- `tests/computer-lane-core-lifecycle.test.ts`
- this ledger

## Explicitly not run / not claimed

No desktop app, activation, foreground/global input, screenshot, native SDK, release packaging, build, smoke, or full project test gate was run. The baseline initially lacked the `tsc` executable; the permitted `bun install --frozen-lockfile` restored local TypeScript without changing manifests/lockfile.

Cross-lane gaps remain: runtime-session admission/cleanup, protocol schema-local window-id decoding, public CLI fallback, E2E supervisor, screenshot permissions, packaged release loop/fd3, generated API drift, and native Background/TCC evidence.

## Focused validation run

- `bun install --frozen-lockfile` — passed; restored missing local `typescript`/Bun type packages without manifest or lock changes.
- `bun run typecheck` — passed.
- `bun test tests/computer-lane-core-lifecycle.test.ts` — passed 13/13 after adding the separate-driver crash case; the 12-case core subset was also repeated three times.
- `bun test tests/computer-use-review-fixes.test.ts tests/computer-session-host.test.ts tests/computer-exec-state.test.ts tests/computer-target-lease.test.ts tests/computer-request-journal.test.ts` — passed 71/71.
- `bun test tests/computer-exec-worker.test.ts tests/computer-exec-lifecycle.test.ts tests/computer-exec-state.test.ts` — passed 33/33.
- `bun test tests/computer-session-protocol.test.ts tests/computer-session-host.test.ts tests/computer-use-session-cli.test.ts` — passed 39/39.
- `bun test tests/computer-use-batch-cli.test.ts tests/computer-batch.test.ts tests/computer-runtime-budget.test.ts` — passed 52/52.
- `git diff --check` — passed; `git diff --cached --quiet` — passed (no staged files).
