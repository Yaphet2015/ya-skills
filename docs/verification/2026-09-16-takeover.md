# Computer-use 接手验证 — 2026-09-16

状态：J1–J6 与后续 R1 代码修复已完成，完整无桌面/打包门禁通过；独立复审结果见文末。原生完整验收仍阻塞，不合入 main 或发布。

## 恢复来源

- 精确基线：`82722ea5e9ab4021b521def8dcae4cf703fe493f`，原 recovery 仓库干净。
- 整合 delta SHA-256：`f980a16268c58c4c63104a0121c2b4a81303b6d454e3f727719340d168cda658`，核验后仅应用一次。
- 恢复检查点：`8339014`。隔离整合仓库：`/tmp/ya-computer-takeover`。
- 从主仓库合入 `ee08d04`，保留 `0.22.0`、紧凑列表、eli10 与 show-pr 更新；整合提交 `bca03ad`。主仓库 main 分支未合入产品改动。
- 实施分组使用独立 worktree、Luna Max；exec、state、observation 分组写入各自文件，整合者处理 E2E fd。
- 运行版本：Bun `1.3.14`，路径 `/tmp/ya-skills-bun-1.3.14/bun-darwin-aarch64/bun`。

## J4：描述符所有权

新增 `tests/computer-supervisor-fd-ownership.test.ts`，运行真实 supervisor 与无桌面 worker。拦截父进程的第一次 stdio close，立即用其他文件实际复用全部三个 fd 号码。完成后验证文件仍可写且内容正确。

修复前：`bun test tests/computer-supervisor-fd-ownership.test.ts`，0 pass / 1 fail，写入替代文件报 `EBADF`。

修复：删除异步清理阶段对已释放 stdio 数字 fd 的重复关闭。提交 `814bc1d`。

修复后：上述测试与 `computer-integration-e2e-spool.test.ts` 共 5 pass / 0 fail；typecheck 通过。

## 原生探针

本轮仅操作自建 `NSPanel`。源码在 [Fixture.swift](evidence/2026-09-16/Fixture.swift)。使用 `.prohibited` 激活策略、`.nonactivatingPanel` 与 `orderFrontRegardless()`；未调用 activate、set frontmost、makeKeyAndOrderFront、Foreground 或全局输入。

受限执行环境不能连接 WindowServer，第一次窗口 ID 为 0，frontmost PID 为 -1；doctor 报 SDK Protocol。这些结果不作为原生能力证据。清理后，按已有窗口授权在可访问桌面的进程中重试。

- doctor：SDK 0.27.0 加载成功；Accessibility 与 Screen Recording 均 true。未修改权限。
- 窗口创建后的记录：PID 25882、windowId 27959、scale 2；initial/current frontmost PID 均 20223，activationChanges 为空。
- 新 observe 命令失败：`undefined is not an object (evaluating 'asyncOpts?.signal.aborted')`。已交由 observation 组修复 SDK 参数适配。
- 现有 SDK 只读探针取得 840×544、scale 2、frameValid=true，窗口 bounds 为 `(80,708,420,272)`。见 [sdk-observe.json](evidence/2026-09-16/sdk-observe.json)。
- 旧 perceive 取得按钮 AX frame `(99,803,142,38)`。对按钮中心对应图像坐标 `(180,228)` 执行一次 Background Coordinates 探针。
- 点击返回 `DriverError.Tool`；同次观察为 `ax_window_unresolved`，按钮计数保持 0。见 [background-coordinate.json](evidence/2026-09-16/background-coordinate.json)。没有前台重试或重复输入。
- 最终记录包含 frontmost 变化至 612、1383；fixture PID 不在激活记录中。但无法把这次运行作为“frontmost 始终不变”的成功证据，也无法从该记录归因变化原因。见 [final-state.json](evidence/2026-09-16/final-state.json)。
- 拒绝后已关闭测试窗口。Background Coordinates 成功仍为阻塞；未据此继续依赖该能力的原生矩阵或性能验收。

## 仍未完成的全计划验收

原生完整矩阵、session-host TCC 归属、native 生命周期/取消、真实模型调用/usage 对比尚未通过。合成基准不能替代这些验收。

## 本轮修复与复现

| 项 | 修复前的具体失败 | 修复与定向证据 |
|---|---|---|
| J1 | 240,000 字节 state/value 正常返回被判 `worker_exit (code=0)` | worker 退出后分块排空到 EOF；截断/缩短控制文件失败关闭；真实 worker 测试覆盖完整与截断终态 |
| J2 | 两个独立未 await RPC 在终态后仍产生 2 次 batch 派发 | 终态关闭 admission，已派发动作收尾，排队动作记 not_run；补已派发/排队混合握手与 observe 结果可解码测试 |
| J3 | 同内容和 no-op 的第三次请求均误返回 failed；无提交归属的 uncertain 历史反而通过 | 状态快照持久化 requestId，与 durable intent 关联；hash 只用于内容校验；调用链显式传递 requestId |
| J5 | worker 提交成功后，load 的 data 缺少 own `__proto__` | 用 own data property 克隆合法 JSON；覆盖 worker→commit→load→下一次 exec |
| J6 host | 取消后的迟到观察返回 completed | 最终发布前复检 signal/deadline；真实 socket 回归要求 interrupted，journal 不含 completed |
| J6 runtime | native read 后保存/派生阶段可越过取消与 deadline | 合并取消信号，预算贯穿后处理与发布；缩图每次子进程取剩余预算 |

状态组修复前命令：

```sh
bun test tests/computer-integration-state-codec.test.ts -t 'identical later|uncertain commit|__proto__'
# 0 pass / 4 fail
bun test tests/computer-session-host-regressions.test.ts
# 0 pass / 1 fail；期望 interrupted，实际 completed
```

修复后状态组：state-codec、host-regressions、exec-state、core-lifecycle 共 38 pass / 0 fail / 129 assertions。exec-terminal 与旧 review-fixes 共 18 pass / 0 fail；整合者调整截断帧的明确 JSON 构造后，terminal 文件 7 pass / 0 fail。

### SDK observe 额外故障已验证修复

适配层只在存在 signal 时向 SDK 传 `{ signal }`，否则不传异步参数对象。deadline 留在 runtime 控制，避免 SDK 解引用不存在的 signal。

新建同样的非激活窗口后，仅运行一次 `observe --mode both --max-dimension 400`，没有输入：成功保存 840×544 原图和 400×259 派生图，AX 为 truncated，image 为 usable。前台 PID 从创建到关闭保持 1383，activationChanges 为空，按钮计数为 0。见 [fixed-observe.json](evidence/2026-09-16/fixed-observe.json) 和 [fixed-observe-state.json](evidence/2026-09-16/fixed-observe-state.json)。这证明只读观察与显式缩图路径；不替代 Background 坐标点击验收。

## 首次整合门禁与复审记录

首次完整门禁在正常工作区路径 `/Users/phaethon/workspace/personal/worktrees/computer-use-takeover-20260916` 运行。API 生成、typecheck 通过，默认测试 624 pass / 15 skip / 2 fail，故未继续打包。

两项失败已定向处理：历史验证链接断言同步至本记录；TERM 清理 fixture 增加安装 trap 后的就绪文件握手，避免只看到 PID 就发送信号。两文件复跑 5 pass / 0 fail。未删断言或屏蔽测试。

在 `/private/tmp` 的早期状态组默认测试另有 pbench 路径断言失败：它把 macOS 临时仓库的 `/private/` 路径视为私有评测路径。切换正常工作区后该项通过，没有修改 pbench。

独立审查累计实跑 73 项定向测试通过，但发现 **R1/P1**：默认截图子进程超时只发 TERM，未确认退出就返回；替换成忽略 TERM 的真实合成子进程后，deadline 返回 100 ms 后仍存活。审查已清理该测试进程。该问题随后在 `82b1f49` 修复，最终复验见下文。

### 原生持久会话只读闭环

同样的非激活 fixture（PID 39668 / window 28459）完成 source CLI 的 `session open → observe --mode both → exec → 相同 requestId exec → session close`。脚本仅为 `state.n = (state.n ?? 0) + 1; return state.n;`，没有调用任何输入 API。

- 真实 session host PID 39687，独立原生 driver 读取 AX/截图成功；后续 exec 最终观察 epoch 与直接 observe 相同。这验证当前 Bun 1.3.14 运行环境下宿主/driver 的实际读取权限路径，不推导其他签名二进制的 TCC 授权归属。
- 第一次 exec 为 completed，value=1，stateVersion=1，stateCommitted=true；重复相同 requestId 返回既有结果。磁盘 state 仍为 version=1 / `{n:1}`，owner=`native-state-1`。
- close 为 closed，driverTerminated=true、leaseReleased=true、unresolvedRequests 为空。
- 创建至关闭 frontmost PID 始终为 20223，activationChanges 为空、按钮计数 0。测试窗口已退出。

证据：[session-open](evidence/2026-09-16/session-open.json)、[session-observe](evidence/2026-09-16/session-observe.json)、[session-exec](evidence/2026-09-16/session-exec.json)、[重复请求](evidence/2026-09-16/session-exec-replay.json)、[状态快照](evidence/2026-09-16/native-session-state.json)、[session-close](evidence/2026-09-16/session-close.json)、[前台记录](evidence/2026-09-16/session-frontmost.json)。

原生输入的投递/取消、Background Coordinates 成功、窗口移动与完整性能矩阵仍未通过，不能将上述只读闭环扩展为完整 native 验收。


## 最终门禁

最终产品提交：`82b1f49`。版本保持 `0.22.0`，Bun `1.3.14`。门禁日志保存在 [evidence/2026-09-16/gates](evidence/2026-09-16/gates)。

| 门禁 | 结果 |
|---|---|
| API 生成 | computer-use 24 / computer-e2e 31 declarations，无生成漂移 |
| typecheck | 通过 |
| 默认测试 | 630 pass / 15 skip / 0 fail；645 tests，2225 assertions |
| package:release --version 0.22.0 | 通过，包含 yk、skills 与 runtime sidecar |
| 三文件 packaged release tests | 12 pass / 0 fail，72 assertions |
| build / smoke | 通过 |
| 打包后 runtime 装配补验 | 4 pass / 0 fail，23 assertions |

默认 15 个 skip：1 个原生 opt-in；3 个 E2E 打包检查、2 个 runtime 装配检查、7 个 session 打包检查、2 个 packaged-live 检查。后四类已由打包后的命令补跑；原生 opt-in 的通用应用枚举用例未运行。本轮原生证据仅限上文自有窗口探针。

R1 修复：Bun/Node 都在取消或超时后执行有界 TERM→KILL 并确认退出；session 跟踪派生工作到清理结束。无法确认退出时保留 cleanup_failed 和目标占用，拒绝后续工作。新增真实子进程回归 3 项，实施组重复 10 次全过。Node 独立复现由 `closedBeforeReturn:false, childAliveAfterResult:true` 转为 `true, false`；复现脚本见 [node-resize-cleanup.mjs](evidence/2026-09-16/node-resize-cleanup.mjs)。

## 合成基准

最终产品版本运行 `bun scripts/bench/computer-use.ts --samples 10 --warmup 2`，四个变体各 10 次测量、2 次预热，均无失败。原始结果：[benchmark.jsonl](evidence/2026-09-16/benchmark.jsonl)。初始化总次数分别为 40、10、1、1。该测试只测合成 runtime 路径；模型调用、token 与 native measurements 仍为 null/false，不能据此宣称真实桌面或模型性能提升。

## 保存与清理

本轮自有 fixture PID 25769、25882、37455、39668 和 native session host 39687 已确认退出。原主仓库已有未跟踪交接文档保留。全部代码与证据保存到本地分支 `computer-use/takeover-20260916`；main 未合入，未推送或发布。


## 最终独立复审

[独立复审报告](2026-09-16-independent-review.md)确认 `82b1f49` 的本轮代码阻塞全部消除。R1 的 Bun 与 Node 原始复现均通过，最终关键回归 29 pass / 0 fail / 136 assertions，包含真实 host→IPC→driver-worker 取消场景。该结论不替代尚未通过的完整原生输入、TCC 归属、原生取消和模型性能验收。
